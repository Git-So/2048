import { MSG_LEVEL } from "./type";

export default class DEFAULT {
  static readonly THEME = "light";
  static readonly RECORD = "";
  static readonly MSG_LEVEL = MSG_LEVEL.INFO;
  static readonly MSG_TEXT = "滑动或点击按钮开始游戏！";

  static readonly MIN_MOVE_SIZE = 60;
}
